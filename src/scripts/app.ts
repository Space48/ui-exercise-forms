/*

$("btn-submit").on( "click", function() {
    var request = $.ajax({
        url: "/customer/account/resetPassword",
        method: "POST",
        data: { //get email from input emailAddress},
        dataType: "json"
      });
       
      request.done(function( msg ) {
        //Add a success class to the password-feedback div and populate content
      });
       
      request.fail(function( jqXHR, textStatus ) {
        //Add a error class to the password-feedback div and populate content
      });
});

*/

